apiVersion: 1

datasources:
  - name: thalamus-mqtt
    type: grafana-mqtt-datasource
    uid: eeyb2q9aigd1cb
    access: proxy
    jsonData:
      uri: "tcp://mosquitto:1883"
      username: "username" # TODO: Change to the real username
    secureJsonData:
      password: "password" # TODO: Change to the real password
